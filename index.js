import React from 'react';
import {AppRegistry} from 'react-native';
import App from './src/App';

const AppWhatsApp = props => (
    <App />
)

AppRegistry.registerComponent('WhatsApp', () => AppWhatsApp);
